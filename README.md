### Code samples generation for EOLANG

This tool generates relatively random EO code samples.

To customize samples generation see `UserConfiguration` class and set your own bounds and output directory.